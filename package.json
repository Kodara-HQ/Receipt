{
  "name": "the-fragrance-universe",
  "private": true,
  "version": "1.0.0",
  "description": "Receipting and sales management for The Fragrance Universe",
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.3",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.13.1"
  },
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n api,web -c magenta,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "build": "npm run build --prefix frontend",
    "deploy": "npm run build && gh-pages -d frontend/dist"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "gh-pages": "^6.3.0"
  }
}