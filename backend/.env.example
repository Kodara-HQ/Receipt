PORT=4000
JWT_SECRET=change-this-to-a-long-random-string

# Optional. Defaults to backend/data (a JSON file, not a database).
# On Render, point this at a persistent disk if you want receipts to survive restarts.
# DATA_DIR=/var/data
