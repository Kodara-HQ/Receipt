{
  "name": "the-fragrance-universe",
  "private": true,
  "version": "1.0.0",
  "description": "Receipting and sales management for Every Fragrance",
  "scripts": {
    "install:all": "npm install --prefix frontend",
    "dev": "concurrently -n api,web -c magenta,cyan \"php -S localhost:4000 -t backend/public backend/public/router.php\" \"npm run dev --prefix frontend\"",
    "dev:api": "php -S localhost:4000 -t backend/public backend/public/router.php",
    "build": "npm run build --prefix frontend",
    "deploy": "npm run build && gh-pages -d frontend/dist"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "gh-pages": "^6.3.0"
  }
}
